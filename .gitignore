# Build artifacts
/bin/
/dist/
/build/
/rootfs/
# Binary built at the repo root (leading slash so it does NOT match the
# cmd/docker-volume-gcepd/ source directory).
/docker-volume-gcepd

# Go
*.test
*.out
coverage.txt

# Editor / OS
.DS_Store
*.swp
